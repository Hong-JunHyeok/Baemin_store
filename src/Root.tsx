import React from "react";
import "./styles/reset.scss";
import App from "./components/App";

const Root = () => {
  return (
    <React.StrictMode>
      <App />
    </React.StrictMode>
  );
};

export default Root;
